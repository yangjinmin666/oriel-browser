name: Site support request
description: Request reusable Oriel support for a website or workflow.
title: "[Site]: "
labels:
  - enhancement
body:
  - type: markdown
    attributes:
      value: |
        Site notes should capture stable, reusable site knowledge rather than one-off task steps. Never include credentials, cookies, tokens, or other secrets.

        Oriel does not accept site notes whose purpose is to defeat a CAPTCHA, a risk-control challenge, or an anti-abuse system, or to drive bulk consequential actions such as mass messaging or applications.

  - type: input
    id: site
    attributes:
      label: Website
      description: Provide the public site name and a stable entry URL.
      placeholder: "Example — https://example.com/"
    validations:
      required: true

  - type: textarea
    id: workflow
    attributes:
      label: Workflow to support
      description: Describe the user goal and the repeatable sequence the agent should be able to complete.
      placeholder: An agent should be able to ...
    validations:
      required: true

  - type: textarea
    id: current_behavior
    attributes:
      label: Current limitation
      description: Explain where the generic Oriel helpers fail, become unreliable, or require repeated discovery.
    validations:
      required: true

  - type: textarea
    id: reusable_knowledge
    attributes:
      label: Reusable site knowledge
      description: List stable URLs, roles, labels, selectors, or page structure that could make this workflow reliable. Do not use pixel coordinates.

  - type: dropdown
    id: authentication
    attributes:
      label: Authentication required?
      options:
        - "No"
        - "Yes"
        - Unsure
    validations:
      required: true

  - type: textarea
    id: evidence
    attributes:
      label: Examples or evidence
      description: Add sanitized snapshots, logs, or screenshots that demonstrate the limitation.

  - type: checkboxes
    id: checks
    attributes:
      label: Before submitting
      options:
        - label: I searched existing issues and site notes for this website or workflow.
          required: true
        - label: I removed all secrets and sensitive browsing data.
          required: true
        - label: This request relies on stable page semantics, not pixel coordinates.
          required: true
        - label: This request does not ask Oriel to bypass a platform's security or anti-abuse controls.
          required: true
